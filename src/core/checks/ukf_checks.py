from core.base_checker import BaseChecker

# ============================================================================
# ПОЯСНЕНИЕ
# ============================================================================
# Меры категории УКФ (Управление конфигурацией) относятся к организационным
# и процессным мерам, связанным с управлением изменениями. Они не могут быть
# проверены программно на уровне рабочей станции и вынесены в ручной опросник.
# ============================================================================


# ============================================================================
# РУЧНЫЕ ПРОВЕРКИ (опросник)
# ============================================================================

# УКФ.1 — Определение лиц, разрешённых на изменения (УЗ-3,2,1)
class UKF1ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("УКФ.1", "Определение лиц, разрешённых на изменения конфигурации", "high", is_manual=True)
        self.required_levels = [1, 2, 3]
        self.question = ("Определён ли перечень лиц (сотрудников), которым разрешено вносить изменения в конфигурацию "
                         "информационной системы и системы защиты персональных данных?")

    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# УКФ.2 — Управление изменениями конфигурации (УЗ-3,2,1)
class UKF2ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("УКФ.2", "Управление изменениями конфигурации", "high", is_manual=True)
        self.required_levels = [1, 2, 3]
        self.question = ("Осуществляется ли управление изменениями конфигурации информационной системы и системы защиты "
                         "персональных данных (регистрация запросов на изменения, утверждение, контроль реализации)?")

    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# УКФ.3 — Анализ воздействия изменений (УЗ-3,2,1)
class UKF3ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("УКФ.3", "Анализ воздействия планируемых изменений", "high", is_manual=True)
        self.required_levels = [1, 2, 3]
        self.question = ("Проводится ли анализ потенциального воздействия планируемых изменений конфигурации на "
                         "обеспечение защиты персональных данных? Согласуются ли изменения с должностным лицом, "
                         "ответственным за безопасность персональных данных?")

    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# УКФ.4 — Документирование изменений (УЗ-3,2,1)
class UKF4ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("УКФ.4", "Документирование изменений конфигурации", "medium", is_manual=True)
        self.required_levels = [1, 2, 3]
        self.question = ("Осуществляется ли документирование информации об изменениях конфигурации информационной системы "
                         "и системы защиты персональных данных (журнал изменений, регистрация версий)?")

    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ФУНКЦИЯ ДЛЯ ПОЛУЧЕНИЯ ВСЕХ ПРОВЕРОК КАТЕГОРИИ УКФ
# ============================================================================

def get_ukf_checkers():
    """Возвращает список всех проверок категории УКФ (Управление конфигурацией)."""
    return [
        UKF1ManualChecker(),
        UKF2ManualChecker(),
        UKF3ManualChecker(),
        UKF4ManualChecker(),
    ]