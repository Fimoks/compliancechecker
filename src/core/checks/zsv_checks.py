from core.base_checker import BaseChecker

# ============================================================================
# ПОЯСНЕНИЕ
# ============================================================================
# Большинство мер категории ЗСВ (Защита среды виртуализации) невозможно
# проверить программно на уровне отдельной рабочей станции, так как они
# относятся к инфраструктуре виртуализации (гипервизору, хосту виртуализации).
# Для рабочей станции, которая не является хостом виртуализации, эти меры
# вынесены в ручной опросник.
# ============================================================================


# ============================================================================
# ЗСВ.1 — ИАФ В ВИРТУАЛЬНОЙ ИНФРАСТРУКТУРЕ
# ============================================================================

class ZSV1ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗСВ.1", "Идентификация и аутентификация в виртуальной инфраструктуре", "high", is_manual=True)
        self.required_levels = [1, 2, 3, 4]
        self.question = ("Реализованы ли идентификация и аутентификация субъектов доступа в виртуальной инфраструктуре "
                        "(администраторов гипервизора, пользователей ВМ)? Используются ли отдельные учётные записи?")
        self.detailed_description = r"""
📌 ЧТО ПРОВЕРЯЕТСЯ:
Ручная проверка — наличие идентификации и аутентификации в среде виртуализации.

✅ ПОЧЕМУ ЭТО ВАЖНО:
- Администраторы гипервизора имеют полный доступ ко всем ВМ
- Несанкционированный доступ может привести к компрометации всех систем
- Требование для всех уровней защищённости

📋 НОРМАТИВНОЕ ТРЕБОВАНИЕ (Приказ ФСТЭК №21 п. XI, мера 1):
В среде виртуализации должна быть реализована идентификация и аутентификация.

🛠️ КАК ОРГАНИЗОВАТЬ:
- Используйте отдельные учётные записи для каждого администратора
- Внедрите многофакторную аутентификацию для доступа к гипервизору
- Используйте централизованное управление доступом (AD, LDAP)
"""
    
    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ЗСВ.2 — УПРАВЛЕНИЕ ДОСТУПОМ В ВИРТУАЛЬНОЙ ИНФРАСТРУКТУРЕ
# ============================================================================

class ZSV2ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗСВ.2", "Управление доступом в виртуальной инфраструктуре", "high", is_manual=True)
        self.required_levels = [1, 2, 3, 4]
        self.question = ("Разграничен ли доступ к объектам виртуальной инфраструктуры (виртуальным машинам, хранилищам, "
                         "сетям) в соответствии с ролями пользователей и администраторов?")
        self.detailed_description = r"""
📌 ЧТО ПРОВЕРЯЕТСЯ:
Ручная проверка — наличие разграничения доступа в среде виртуализации.

✅ ПОЧЕМУ ЭТО ВАЖНО:
- Разные администраторы должны иметь разные права
- Обычные пользователи не должны управлять ВМ
- Важно ограничить доступ к хранилищам образов ВМ

📋 НОРМАТИВНОЕ ТРЕБОВАНИЕ (Приказ ФСТЭК №21 п. XI, мера 2):
В среде виртуализации должно быть реализовано управление доступом.

🛠️ КАК ОРГАНИЗОВАТЬ:
- Используйте ролевую модель (RBAC) в гипервизоре
- Назначайте минимально необходимые привилегии
- Отделите права администратора ВМ от прав администратора гипервизора
"""
    
    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ЗСВ.3 — РЕГИСТРАЦИЯ СОБЫТИЙ В ВИРТУАЛЬНОЙ ИНФРАСТРУКТУРЕ
# ============================================================================

class ZSV3ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗСВ.3", "Регистрация событий безопасности в виртуальной инфраструктуре", "medium", is_manual=True)
        self.required_levels = [1, 2, 3]
        self.question = ("Ведётся ли регистрация событий безопасности на уровне гипервизора (логи доступа, изменений "
                         "конфигурации ВМ, запуска/остановки ВМ)?")
        self.detailed_description = r"""
📌 ЧТО ПРОВЕРЯЕТСЯ:
Ручная проверка — наличие регистрации событий в среде виртуализации.

✅ ПОЧЕМУ ЭТО ВАЖНО:
- Позволяет выявлять несанкционированные действия с ВМ
- Необходимо для расследования инцидентов
- Обязательное требование для УЗ-1,2,3

📋 НОРМАТИВНОЕ ТРЕБОВАНИЕ (Приказ ФСТЭК №21 п. XI, мера 3):
В среде виртуализации должна осуществляться регистрация событий безопасности.

🛠️ КАК ОРГАНИЗОВАТЬ:
- Включите аудит в консоли управления гипервизора
- Настройте централизованный сбор логов (syslog, SIEM)
- Регистрируйте: запуск/остановку ВМ, изменения прав, подключения к консоли
"""
    
    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ЗСВ.6 — УПРАВЛЕНИЕ ПЕРЕМЕЩЕНИЕМ ВМ
# ============================================================================

class ZSV6ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗСВ.6", "Управление перемещением виртуальных машин", "medium", is_manual=True)
        self.required_levels = [1, 2]
        self.question = ("Контролируется ли перемещение виртуальных машин (live migration) между физическими хостами? "
                         "Осуществляется ли шифрование каналов при миграции?")
        self.detailed_description = r"""
📌 ЧТО ПРОВЕРЯЕТСЯ:
Ручная проверка — контроль миграции виртуальных машин.

✅ ПОЧЕМУ ЭТО ВАЖНО:
- Несанкционированная миграция может нарушить политики безопасности
- Трафик миграции может быть перехвачен злоумышленником
- Обязательное требование для УЗ-1,2

📋 НОРМАТИВНОЕ ТРЕБОВАНИЕ (Приказ ФСТЭК №21 п. XI, мера 6):
Должно осуществляться управление перемещением виртуальных машин.

🛠️ КАК ОРГАНИЗОВАТЬ:
- Включите шифрование трафика миграции (TLS)
- Ограничьте live migration только для авторизованных администраторов
- Используйте выделенные сети для миграции
"""
    
    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ЗСВ.7 — КОНТРОЛЬ ЦЕЛОСТНОСТИ ВИРТУАЛЬНОЙ ИНФРАСТРУКТУРЫ
# ============================================================================

class ZSV7ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗСВ.7", "Контроль целостности виртуальной инфраструктуры", "high", is_manual=True)
        self.required_levels = [1, 2]
        self.question = ("Осуществляется ли контроль целостности конфигураций гипервизора и виртуальных машин "
                         "(контроль изменений, защита от подмены образов ВМ)?")
        self.detailed_description = r"""
📌 ЧТО ПРОВЕРЯЕТСЯ:
Ручная проверка — контроль целостности конфигураций виртуальной среды.

✅ ПОЧЕМУ ЭТО ВАЖНО:
- Подмена образа ВМ может привести к внедрению вредоносного ПО
- Изменение конфигурации может ослабить безопасность
- Обязательное требование для УЗ-1,2

📋 НОРМАТИВНОЕ ТРЕБОВАНИЕ (Приказ ФСТЭК №21 п. XI, мера 7):
Должен осуществляться контроль целостности виртуальной инфраструктуры.

🛠️ КАК ОРГАНИЗОВАТЬ:
- Используйте контроль версий конфигураций
- Настройте аудит изменений в гипервизоре
- Применяйте цифровые подписи для образов ВМ
"""
    
    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ЗСВ.8 — РЕЗЕРВНОЕ КОПИРОВАНИЕ В ВИРТУАЛЬНОЙ ИНФРАСТРУКТУРЕ
# ============================================================================

class ZSV8ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗСВ.8", "Резервное копирование в виртуальной инфраструктуре", "high", is_manual=True)
        self.required_levels = [1, 2]
        self.question = ("Настроено ли резервное копирование виртуальных машин, образов ВМ, конфигураций гипервизора? "
                         "Проводится ли регулярное тестирование восстановления?")
        self.detailed_description = r"""
📌 ЧТО ПРОВЕРЯЕТСЯ:
Ручная проверка — наличие резервного копирования виртуальной инфраструктуры.

✅ ПОЧЕМУ ЭТО ВАЖНО:
- Потеря образов ВМ приводит к недоступности всех сервисов
- Восстановление из бэкапов критически важно
- Обязательное требование для УЗ-1,2

📋 НОРМАТИВНОЕ ТРЕБОВАНИЕ (Приказ ФСТЭК №21 п. XI, мера 8):
Должно осуществляться резервное копирование в виртуальной инфраструктуре.

🛠️ КАК ОРГАНИЗОВАТЬ:
- Используйте специализированные средства (Veeam, Acronis, VMware Data Protection)
- Храните бэкапы ВМ отдельно от гипервизора
- Регулярно тестируйте восстановление
"""
    
    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ЗСВ.9 — АНТИВИРУСНАЯ ЗАЩИТА В ВИРТУАЛЬНОЙ ИНФРАСТРУКТУРЕ
# ============================================================================

class ZSV9ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗСВ.9", "Антивирусная защита в виртуальной инфраструктуре", "medium", is_manual=True)
        self.required_levels = [1, 2, 3]
        self.question = ("Реализована ли антивирусная защита на уровне гипервизора и виртуальных машин? "
                         "Используется ли специализированное AV-решение для виртуальных сред (без агентов в ВМ)?")
        self.detailed_description = r"""
📌 ЧТО ПРОВЕРЯЕТСЯ:
Ручная проверка — наличие антивирусной защиты в виртуальной среде.

✅ ПОЧЕМУ ЭТО ВАЖНО:
- Вирусы могут распространяться между ВМ через общие ресурсы
- Агенты в каждой ВМ создают высокую нагрузку
- Обязательное требование для УЗ-1,2,3

📋 НОРМАТИВНОЕ ТРЕБОВАНИЕ (Приказ ФСТЭК №21 п. XI, мера 9):
Должна быть реализована антивирусная защита в виртуальной инфраструктуре.

🛠️ КАК ОРГАНИЗОВАТЬ:
- Используйте агентные решения (Kaspersky, Dr.Web в каждой ВМ)
- Используйте без-агентные решения (vShield, NSX)
- Регулярно обновляйте антивирусные базы
"""
    
    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ЗСВ.10 — СЕГМЕНТИРОВАНИЕ ВИРТУАЛЬНОЙ ИНФРАСТРУКТУРЫ
# ============================================================================

class ZSV10ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗСВ.10", "Сегментирование виртуальной инфраструктуры", "medium", is_manual=True)
        self.required_levels = [1, 2, 3]
        self.question = ("Разбита ли виртуальная инфраструктура на сегменты для обработки персональных данных "
                         "отдельными пользователями или группами пользователей (изоляция ВМ)?")
        self.detailed_description = r"""
📌 ЧТО ПРОВЕРЯЕТСЯ:
Ручная проверка — наличие сегментирования виртуальной сети.

✅ ПОЧЕМУ ЭТО ВАЖНО:
- Изоляция ВМ разных пользователей предотвращает утечки
- Сегментирование ограничивает распространение атак
- Обязательное требование для УЗ-1,2,3

📋 НОРМАТИВНОЕ ТРЕБОВАНИЕ (Приказ ФСТЭК №21 п. XI, мера 10):
Должно быть реализовано сегментирование виртуальной инфраструктуры.

🛠️ КАК ОРГАНИЗОВАТЬ:
- Используйте виртуальные локальные сети (VLAN)
- Применяйте микросегментацию (NSX, OpenStack)
- Изолируйте ВМ с разными уровнями конфиденциальности
"""
    
    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ФУНКЦИЯ ДЛЯ ПОЛУЧЕНИЯ ВСЕХ ПРОВЕРОК КАТЕГОРИИ ЗСВ
# ============================================================================

def get_zsv_checkers():
    """Возвращает список всех проверок категории ЗСВ (Защита среды виртуализации)."""
    return [
        ZSV1ManualChecker(),
        ZSV2ManualChecker(),
        ZSV3ManualChecker(),
        ZSV6ManualChecker(),
        ZSV7ManualChecker(),
        ZSV8ManualChecker(),
        ZSV9ManualChecker(),
        ZSV10ManualChecker(),
    ]