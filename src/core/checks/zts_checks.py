from core.base_checker import BaseChecker

# ============================================================================
# ПОЯСНЕНИЕ
# ============================================================================
# Меры категории ЗТС (Защита технических средств) относятся к физической
# защите оборудования, контролируемой зоне, размещению устройств вывода и
# защите от внешних воздействий. Они не могут быть проверены программно на
# уровне рабочей станции и вынесены в ручной опросник.
# ============================================================================


# ============================================================================
# РУЧНЫЕ ПРОВЕРКИ (опросник)
# ============================================================================

# ЗТС.3 — Контроль и управление физическим доступом (все уровни)
class ZTS3ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗТС.3", "Контроль физического доступа к техническим средствам", "high", is_manual=True)
        self.required_levels = [1, 2, 3, 4]
        self.question = ("Осуществляется ли контроль физического доступа в помещения, где установлены технические средства, "
                         "обрабатывающие персональные данные (пропускная система, охрана, видеонаблюдение)?")

    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ЗТС.4 — Размещение устройств вывода информации (все уровни)
class ZTS4ManualChecker(BaseChecker):
    def __init__(self):
        super().__init__("ЗТС.4", "Размещение устройств вывода информации", "medium", is_manual=True)
        self.required_levels = [1, 2, 3, 4]
        self.question = ("Размещены ли устройства вывода информации (мониторы, проекторы) таким образом, "
                         "чтобы исключить несанкционированный просмотр персональных данных посторонними лицами? "
                         "(установка мониторов спиной к окну, использование защитных экранов, ограничение доступа)")

    def check(self) -> dict:
        return self._get_result(False, None, self.question)


# ============================================================================
# ФУНКЦИЯ ДЛЯ ПОЛУЧЕНИЯ ВСЕХ ПРОВЕРОК КАТЕГОРИИ ЗТС
# ============================================================================

def get_zts_checkers():
    """Возвращает список всех проверок категории ЗТС (Защита технических средств)."""
    return [
        ZTS3ManualChecker(),
        ZTS4ManualChecker(),
    ]